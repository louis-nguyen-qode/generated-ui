// id="635:30778"
import React, { CSSProperties } from "react"
import { Flex } from "antd"
import { Icon } from "@designSystem/icon"

import QodeSvg63530795 from "@designSystem/icon/svg/QodeSvg63530795"
import QodeSvg63530796 from "@designSystem/icon/svg/QodeSvg63530796"
import QodeSvg63530780 from "@designSystem/icon/svg/QodeSvg63530780"
import QodeSvg63530798 from "@designSystem/icon/svg/QodeSvg63530798"
import QodeSvg63530799 from "@designSystem/icon/svg/QodeSvg63530799"
import QodeSvg63530782 from "@designSystem/icon/svg/QodeSvg63530782"
import QodeSvg63530801 from "@designSystem/icon/svg/QodeSvg63530801"
import QodeSvg63530802 from "@designSystem/icon/svg/QodeSvg63530802"
import QodeSvg63530784 from "@designSystem/icon/svg/QodeSvg63530784"
import QodeSvg63530804 from "@designSystem/icon/svg/QodeSvg63530804"
import QodeSvg63530805 from "@designSystem/icon/svg/QodeSvg63530805"
import QodeSvg63530786 from "@designSystem/icon/svg/QodeSvg63530786"
import QodeSvg63530787 from "@designSystem/icon/svg/QodeSvg63530787"
import QodeSvg63530788 from "@designSystem/icon/svg/QodeSvg63530788"
import QodeSvg63530789 from "@designSystem/icon/svg/QodeSvg63530789"
import QodeSvg63530790 from "@designSystem/icon/svg/QodeSvg63530790"
import QodeSvg63530791 from "@designSystem/icon/svg/QodeSvg63530791"
import QodeSvg63530792 from "@designSystem/icon/svg/QodeSvg63530792"
import QodeSvg63530793 from "@designSystem/icon/svg/QodeSvg63530793"
import QodeSvg63530794 from "@designSystem/icon/svg/QodeSvg63530794"

export interface QodeCustom63530778Props {
  type?: "Default" | "mono"
  darkmode?: "yes" | "no"
  orientation?: "portrait" | "landscape"
  wordmark?: "yes" | "no"
}

export const QodeCustom63530778: React.FC<
  QodeCustom63530778Props & { style?: CSSProperties }
> = ({
  type = "Default",
  darkmode = "yes",
  orientation = "landscape",
  wordmark = "yes",
  ...rest
}) => {
  return (
    <>
      {/* name="logo" id="635:30778" type="COMPONENT_SET" */}
      {/* id="635:30779" */}
      {`${type}` === `Default` &&
        `${darkmode}` === `yes` &&
        `${orientation}` === `landscape` &&
        `${wordmark}` === `yes` && (
          <>
            {/* name="type=Default, ?darkmode=yes, orientation=landscape, ?wordmark=yes" id="635:30779" type="COMPONENT" */}
            <div
              style={{
                display: "flex",
                width: "294px",
                height: "99px",
                justifyContent: "center",
                alignItems: "center",
                gap: "16px",
                flexShrink: "0",
              }}
            >
              {/* name="symbol" id="635:30795" type="VECTOR" */}
              <QodeSvg63530795
                style={{
                  width: "114px",
                  height: "99px",
                  flexShrink: "0",
                  fill: "var(--primary-primary-50, #1597f4)",
                  strokeWidth: "2px",
                  stroke: "var(--border-border-primary-20, #158de2)",
                }}
                {...{}}
              />
              {/* name="wordmark" id="635:30796" type="VECTOR" */}
              <QodeSvg63530796
                style={{
                  width: "164px",
                  height: "65px",
                  flexShrink: "0",
                  fill: "var(--brands-black, #000)",
                }}
                {...{}}
              />
            </div>
          </>
        )}
      {/* id="635:30780" */}
      {`${type}` === `Default` &&
        `${darkmode}` === `yes` &&
        `${orientation}` === `landscape` &&
        `${wordmark}` === `no` && (
          <>
            {/* name="type=Default, ?darkmode=yes, orientation=landscape, ?wordmark=no" id="635:30780" type="COMPONENT" */}
            <QodeSvg63530780
              style={{ width: "100px", height: "100px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30781" */}
      {`${type}` === `Default` &&
        `${darkmode}` === `no` &&
        `${orientation}` === `landscape` &&
        `${wordmark}` === `yes` && (
          <>
            {/* name="type=Default, ?darkmode=no, orientation=landscape, ?wordmark=yes" id="635:30781" type="COMPONENT" */}
            <div
              style={{
                display: "flex",
                width: "294px",
                height: "99px",
                justifyContent: "center",
                alignItems: "center",
                gap: "16px",
                flexShrink: "0",
              }}
            >
              {/* name="symbol" id="635:30798" type="VECTOR" */}
              <QodeSvg63530798
                style={{
                  width: "114px",
                  height: "99px",
                  flexShrink: "0",
                  fill: "var(--primary-primary-50, #1597f4)",
                  strokeWidth: "2px",
                  stroke: "var(--border-border-primary-20, #158de2)",
                }}
                {...{}}
              />
              {/* name="wordmark" id="635:30799" type="VECTOR" */}
              <QodeSvg63530799
                style={{
                  width: "164px",
                  height: "65px",
                  flexShrink: "0",
                  fill: "var(--brands-white, #fff)",
                }}
                {...{}}
              />
            </div>
          </>
        )}
      {/* id="635:30782" */}
      {`${type}` === `Default` &&
        `${darkmode}` === `no` &&
        `${orientation}` === `landscape` &&
        `${wordmark}` === `no` && (
          <>
            {/* name="type=Default, ?darkmode=no, orientation=landscape, ?wordmark=no" id="635:30782" type="COMPONENT" */}
            <QodeSvg63530782
              style={{ width: "100px", height: "100px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30783" */}
      {`${type}` === `mono` &&
        `${darkmode}` === `yes` &&
        `${orientation}` === `landscape` &&
        `${wordmark}` === `yes` && (
          <>
            {/* name="type=mono, ?darkmode=yes, orientation=landscape, ?wordmark=yes" id="635:30783" type="COMPONENT" */}
            <div
              style={{
                display: "flex",
                width: "294px",
                height: "99px",
                justifyContent: "center",
                alignItems: "center",
                gap: "16px",
                flexShrink: "0",
              }}
            >
              {/* name="symbol" id="635:30801" type="VECTOR" */}
              <QodeSvg63530801
                style={{
                  width: "114px",
                  height: "99px",
                  flexShrink: "0",
                  fill: "var(--brands-black, #000)",
                  filter: "drop-shadow(0px 4px 4px rgba(0, 0, 0, 0.25))",
                }}
                {...{}}
              />
              {/* name="wordmark" id="635:30802" type="VECTOR" */}
              <QodeSvg63530802
                style={{
                  width: "164px",
                  height: "65px",
                  flexShrink: "0",
                  fill: "var(--brands-black, #000)",
                }}
                {...{}}
              />
            </div>
          </>
        )}
      {/* id="635:30784" */}
      {`${type}` === `mono` &&
        `${darkmode}` === `yes` &&
        `${orientation}` === `landscape` &&
        `${wordmark}` === `no` && (
          <>
            {/* name="type=mono, ?darkmode=yes, orientation=landscape, ?wordmark=no" id="635:30784" type="COMPONENT" */}
            <QodeSvg63530784
              style={{ width: "100px", height: "100px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30785" */}
      {`${type}` === `mono` &&
        `${darkmode}` === `no` &&
        `${orientation}` === `landscape` &&
        `${wordmark}` === `yes` && (
          <>
            {/* name="type=mono, ?darkmode=no, orientation=landscape, ?wordmark=yes" id="635:30785" type="COMPONENT" */}
            <div
              style={{
                display: "flex",
                width: "294px",
                height: "99px",
                justifyContent: "center",
                alignItems: "center",
                gap: "16px",
                flexShrink: "0",
              }}
            >
              {/* name="symbol" id="635:30804" type="VECTOR" */}
              <QodeSvg63530804
                style={{
                  width: "114px",
                  height: "99px",
                  flexShrink: "0",
                  fill: "var(--brands-white, #fff)",
                  strokeWidth: "1px",
                  stroke: "var(--border-border-primary-20, #158de2)",
                }}
                {...{}}
              />
              {/* name="wordmark" id="635:30805" type="VECTOR" */}
              <QodeSvg63530805
                style={{
                  width: "164px",
                  height: "65px",
                  flexShrink: "0",
                  fill: "var(--brands-white, #fff)",
                }}
                {...{}}
              />
            </div>
          </>
        )}
      {/* id="635:30786" */}
      {`${type}` === `mono` &&
        `${darkmode}` === `no` &&
        `${orientation}` === `landscape` &&
        `${wordmark}` === `no` && (
          <>
            {/* name="type=mono, ?darkmode=no, orientation=landscape, ?wordmark=no" id="635:30786" type="COMPONENT" */}
            <QodeSvg63530786
              style={{ width: "100px", height: "100px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30787" */}
      {`${type}` === `Default` &&
        `${darkmode}` === `yes` &&
        `${orientation}` === `portrait` &&
        `${wordmark}` === `yes` && (
          <>
            {/* name="type=Default, ?darkmode=yes, orientation=portrait, ?wordmark=yes" id="635:30787" type="COMPONENT" */}
            <QodeSvg63530787
              style={{ width: "164px", height: "180px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30788" */}
      {`${type}` === `Default` &&
        `${darkmode}` === `yes` &&
        `${orientation}` === `portrait` &&
        `${wordmark}` === `no` && (
          <>
            {/* name="type=Default, ?darkmode=yes, orientation=portrait, ?wordmark=no" id="635:30788" type="COMPONENT" */}
            <QodeSvg63530788
              style={{ width: "100px", height: "100px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30789" */}
      {`${type}` === `Default` &&
        `${darkmode}` === `no` &&
        `${orientation}` === `portrait` &&
        `${wordmark}` === `yes` && (
          <>
            {/* name="type=Default, ?darkmode=no, orientation=portrait, ?wordmark=yes" id="635:30789" type="COMPONENT" */}
            <QodeSvg63530789
              style={{ width: "164px", height: "180px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30790" */}
      {`${type}` === `Default` &&
        `${darkmode}` === `no` &&
        `${orientation}` === `portrait` &&
        `${wordmark}` === `no` && (
          <>
            {/* name="type=Default, ?darkmode=no, orientation=portrait, ?wordmark=no" id="635:30790" type="COMPONENT" */}
            <QodeSvg63530790
              style={{ width: "100px", height: "100px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30791" */}
      {`${type}` === `mono` &&
        `${darkmode}` === `yes` &&
        `${orientation}` === `portrait` &&
        `${wordmark}` === `yes` && (
          <>
            {/* name="type=mono, ?darkmode=yes, orientation=portrait, ?wordmark=yes" id="635:30791" type="COMPONENT" */}
            <QodeSvg63530791
              style={{ width: "164px", height: "180px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30792" */}
      {`${type}` === `mono` &&
        `${darkmode}` === `yes` &&
        `${orientation}` === `portrait` &&
        `${wordmark}` === `no` && (
          <>
            {/* name="type=mono, ?darkmode=yes, orientation=portrait, ?wordmark=no" id="635:30792" type="COMPONENT" */}
            <QodeSvg63530792
              style={{ width: "100px", height: "100px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30793" */}
      {`${type}` === `mono` &&
        `${darkmode}` === `no` &&
        `${orientation}` === `portrait` &&
        `${wordmark}` === `yes` && (
          <>
            {/* name="type=mono, ?darkmode=no, orientation=portrait, ?wordmark=yes" id="635:30793" type="COMPONENT" */}
            <QodeSvg63530793
              style={{ width: "164px", height: "180px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
      {/* id="635:30794" */}
      {`${type}` === `mono` &&
        `${darkmode}` === `no` &&
        `${orientation}` === `portrait` &&
        `${wordmark}` === `no` && (
          <>
            {/* name="type=mono, ?darkmode=no, orientation=portrait, ?wordmark=no" id="635:30794" type="COMPONENT" */}
            <QodeSvg63530794
              style={{ width: "100px", height: "100px", flexShrink: "0" }}
              {...{}}
            />
          </>
        )}
    </>
  )
}
